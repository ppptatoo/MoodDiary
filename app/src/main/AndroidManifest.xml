<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 网络权限：AI接口调用必须 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 存储权限：保存AI生成的图片 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!-- 在Android 10及以上，使用媒体库访问 -->
    <uses-permission android:name="android.permission.ACCESS_MEDIA_LOCATION" />


    <application
        android:name=".MoodDiaryApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MoodDiary">

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <activity
            android:name=".ui.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.RegisterActivity" android:exported="false"/>

        <activity android:name=".ui.HomeActivity" android:exported="false"/>
        <activity android:name=".ui.DetailActivity" android:exported="false"/>
        <activity android:name=".ui.DiaryListActivity" android:exported="false"/>
        <activity android:name=".ui.StatisticsActivity" android:exported="false"/>
        <activity android:name=".ui.DiaryDetailActivity" android:exported="false"/>
        <activity android:name=".ui.ProfileActivity" />
        <activity android:name=".ui.ProfileDetailActivity" android:exported="false"/>
        <activity android:name=".ui.NotificationSettingsActivity" android:exported="false"/>
        <activity android:name=".ui.ThemeSettingsActivity" android:exported="false"/>
        <activity android:name=".ui.AboutUsActivity" android:exported="false"/>
        <activity android:name=".ui.PrivacyPolicyActivity" android:exported="false"/>
        <activity android:name=".ui.MyFavoritesActivity" android:exported="false"/>
        <activity android:name=".ui.CalendarActivity" android:exported="false"/>
        <activity android:name=".ui.AiChatActivity" android:exported="false"/>
    </application>

</manifest>